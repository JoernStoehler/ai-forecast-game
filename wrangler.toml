name = "ai-forecast-game"
main = "worker/index.ts"
compatibility_date = "2025-02-01"

# D1 Database
[[d1_databases]]
binding = "DB"
database_name = "ai-forecast-game-db"
database_id = "" # Fill after: wrangler d1 create ai-forecast-game-db

# Custom domain (configure in Cloudflare dashboard)
# routes = [{ pattern = "<tbd>.joernstoehler.com", custom_domain = true }]

# [vars]
# ENVIRONMENT = "production"
